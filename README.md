# 🤖 LangGraph Agentic AI System

## 📖 Overview

This project implements a **Agentic AI system** using LangGraph, LangChain, and FastAPI.

It combines:

* 🔧 Tool-calling agents (web search)
* 🔀 Multi-agent routing (math vs search)
* ⚡ FastAPI for real-time API serving

The system intelligently decides how to process a query and routes it to the appropriate agent.

---

## 🧠 Architecture

```
User Query
   ↓
Router Agent (LLM-based decision)
   ↓
 ┌───────────────┐
 ↓               ↓
Math Agent     Search Agent (Tool Calling)
 ↓               ↓
      Final Response
```

---

## 🚀 Features

* ✅ Tool-calling agent using LangChain
* ✅ Dynamic routing using LangGraph
* ✅ Multi-agent architecture
* ✅ FastAPI REST API
* ✅ Pydantic schemas for validation
* ✅ Environment-based configuration (conda)


---

## 🛠️ Tech Stack

* Python
* LangGraph
* LangChain
* Google Gemini (LLM)
* FastAPI
* Pydantic
* Requests (for web search)

---

## 📂 Project Structure

```
app/
├── main.py
├── api/
│   └── routes.py
├── agents/
│   ├── search_agent.py
│   ├── math_agent.py
│   └── router_agent.py
├── tools/
│   └── search_tool.py
├── graph/
│   └── workflow.py
├── schemas/
│   └── request.py
├── core/
│   └── config.py
```

---

## ⚙️ Installation

```bash
pip install -r requirements.txt
```

---

## 🔐 Setup Environment Variables

Create a `.env` file:

```
GOOGLE_API_KEY=
SERPER_API_KEY=
```

---

## ▶️ Run the Application

```bash
uvicorn app.main:app --reload
```

---

## 🌐 API Usage

### Endpoint:

```
POST /query
```

### Request Body:

```json
{
  "query": "Who won IPL 2025?"
}
```

### Response:

```json
{
  "response": "Answer generated by agent"
}
```

---

## 📊 Example Queries

* "Who won the IPL 2025 final?"
* "Solve 3x + 12x = 40"
* "Latest news about AI"

---

## 🧪 How It Works

### 1. Router Agent

* Uses LLM to classify query
* Decides: `math` or `search`

### 2. Math Agent

* Solves mathematical queries using LLM

### 3. Search Agent

* Uses tool-calling agent
* Calls Serper API for real-time results

---

## 🔥 Key Concepts Used

* ReAct-style tool usage (modern tool-calling agent)
* Stateful workflows using LangGraph
* Conditional edges (dynamic routing)
* Modular agent design

---

## 🧪 Future Improvements

* Add memory (conversation context)
* Add more specialized agents
* Streaming responses
* Deploy on cloud (AWS / Render)
* Add authentication & rate limiting

---


---


